### Editor tests

#### <ClassName>

| Test Method                           | Verification                                  |
|---------------------------------------|-----------------------------------------------|
| `Method_条件_期待結果`                | `<プロパティ>` が `<期待値または状態>` である |
| `Method_条件_期待結果` (再現テスト)   | `<プロパティ>` が `<期待値または状態>` である |
| `Method_条件_期待結果` (n: {3, 6, 9}) | `<プロパティ>` が `<期待値または状態>` である |

### Unit tests

#### <ClassName>

| Test Method            | Verification                                                          |
|------------------------|-----------------------------------------------------------------------|
| `Method_条件_期待結果` | `<プロパティ>` が `<期待値または状態>` である                         |
| `Method_条件_期待結果` | `<プロパティ>` が `<期待値または状態>` である (uses spy: IDependency) |

### Integration tests

#### <ClassName>

| Test Method     | Verification                                  |
|-----------------|-----------------------------------------------|
| `条件_期待結果` | `<プロパティ>` が `<期待値または状態>` である |

### Visual verification tests

#### <ClassName>

| Test Method     | Image analysis by saved screenshot                                                                                            |
|-----------------|-------------------------------------------------------------------------------------------------------------------------------|
| `条件_期待結果` | <Section 4 の観点リストに従う — 例: 要素の画面内位置、要素間の重なりなし、文字と背景のコントラスト、フォントサイズ・スタイル> |

### Manual tests

| Test Case                | Test perspectives / Verification method |
|--------------------------|-----------------------------------------|
| テストケースの簡潔な説明 | <検証する挙動と確認方法>                |
